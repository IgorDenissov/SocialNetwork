import React from 'react';
import classes from './Header.module.css';

const Header = () => {
  return (
      <header className = {classes.header}>
          <img src="" alt="Ops img is missing"/>
      </header>
  );
}

export default Header;
